# Client App

### Install Requirements

```ps
python3 -m pip install -r requirements.txt
```
### Things to change befor running

In the .env file change ccserver env variable to the url of the server running the command and control server.

### Running the client

```ps
python3 localserv.py
```
